{
  "name": "lottie-spike-probe",
  "private": true,
  "version": "0.0.0",
  "description": "Throwaway probe validating whether the pure-JS lottie.js renderer can render headlessly (no DOM/WASM) in Node + Worker + browser OffscreenCanvas contexts, per the Roku OS 15.3 roAnimatedImage/AnimatedImage plan.",
  "dependencies": {
    "canvas": "^2.11.2",
    "lottie.js": "^0.4.0"
  }
}
